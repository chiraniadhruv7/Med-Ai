# MedGemma inference mode: "local" (MLX) or "api" (Google Gemini)
MEDGEMMA_MODE=local

# Google Gemini API key (required only if MEDGEMMA_MODE=api)
GEMINI_API_KEY=

# Server config
BACKEND_HOST=127.0.0.1
BACKEND_PORT=8000
